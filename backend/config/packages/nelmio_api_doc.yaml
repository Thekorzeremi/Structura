nelmio_api_doc:
    documentation:
        info:
            title: Structura API
            description: API for managing worksites, events and users
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)
